class PaperScanner {

  calculate(sum) {
    let paperResult = []
    let total = 0
    total += (parseInt(sum, 10))
    paperResult.push(sum, total)
    // let splitSum = sum.split(" ")
    return paperResult
  }


}

module.exports = PaperScanner